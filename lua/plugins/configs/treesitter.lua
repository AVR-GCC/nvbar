local options = {
  ensure_installed = { "javascript", "typescript", "python", "lua", "vim", "vimdoc" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
